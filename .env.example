# Solana RPC URL
SOLANA_RPC_URL=http://localhost:8899

# Pinata JWT for IPFS uploads
VITE_PINATA_JWT=

# Worker keypair path (will be auto-generated if not exists)
WORKER_KEYPAIR_PATH=./worker-keypair.json
